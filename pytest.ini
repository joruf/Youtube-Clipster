[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers -ra
markers =
    gui: needs a display and tkinter; skipped automatically when none is available
    network: talks to YouTube; deselected by default, run with -m network
filterwarnings =
    error::DeprecationWarning:clipster.*
